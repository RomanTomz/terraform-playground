# Hands-on Exercises

These exercises focus on **intent + practice**.

## Exercise 1: S3 unique naming
Intent: Apply s3-bucket with custom prefix and verify random suffix behavior.

Suggested commands:
```bash
terraform init
terraform plan
terraform apply
terraform destroy
```

## Exercise 2: IAM least privilege
Intent: Read policy JSON and explain why only ListAllMyBuckets is allowed.

Suggested commands:
```bash
terraform init
terraform plan
terraform apply
terraform destroy
```

## Exercise 3: SG safety baseline
Intent: Create SG in test VPC and verify no ingress rules are present.

Suggested commands:
```bash
terraform init
terraform plan
terraform apply
terraform destroy
```
