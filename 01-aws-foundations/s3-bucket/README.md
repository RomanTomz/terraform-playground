# AWS Foundations - S3 Bucket

## Lesson intent
Create a safe first AWS resource and understand globally unique naming.

## Sports analogy
This is like assigning a jersey number that must be unique league-wide, so we add a random suffix.

⚠️ **Cost warning:** S3 is low-cost but not free in all usage patterns.

Cleanup: run `terraform destroy` when done.
